# Contributing

Use Python 3.11+, keep the standard-library-only runtime, add a focused test for non-trivial behavior, and run `python -m unittest discover -s tests` plus `python -m compileall src scripts` before opening a pull request. Never commit credentials or local configuration.
